interface:
  display_name: "Sirno Editor"
  short_description: "Edit Sirno-managed codebases from the lake first"
  default_prompt: "Use $sirno-editor to start this edit in the lake, sync entries and witnesses, then validate."
